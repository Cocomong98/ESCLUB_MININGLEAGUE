# Mockup Data

This folder contains minimal sample files for local bootstrapping.
Runtime crawl data should be stored outside git-tracked files.

Expected runtime paths:
- `/data/<season>/current_crawl_display_data.json`
- `/data/<season>/user/<player_id>/<player_id>_YYMMDD.json`
